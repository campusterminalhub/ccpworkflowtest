name: Generate YAML Script

on:
  push:
    branches:
      - main  # Replace with the branch you want to trigger this workflow on

jobs:
  generate_yaml:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Generate YAML
        run: |
          # Replace this with your script to generate the YAML content
          echo "This is a sample YAML file generated by the workflow" > generated.yaml

      - name: Commit and push changes
        run: |
          git config user.name "campusterminalhub"
          git config user.email "jjagadeeshgouda1996@gmail.com"
          git add generated.yaml
          git commit -m "Generate YAML file"
          git remote add origin_with_token https://$GITHUB_ACTOR:${{ secrets.YOUR_PAT_SECRET }}@github.com/campusterminalhub/ccpflow.git
          git push
